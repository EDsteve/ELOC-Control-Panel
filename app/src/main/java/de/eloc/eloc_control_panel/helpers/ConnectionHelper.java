package de.eloc.eloc_control_panel.helpers;

public class ConnectionHelper {
    private ConnectionHelper() {
        // Class must not be instantiated  - use private constructor
    }

    // I was going to use a single file (this one to keep track of the connections)
    // I have a feeling the code might get mess.. but I will just do the disconnect and reconnect
    // if the behavior is unpredictable, then we can organize the code in this file.
}